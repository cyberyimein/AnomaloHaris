{
  "name": "anomaloharis",
  "version": "0.1.0",
  "private": true,
  "description": "AnomaloHaris shared TypeScript workspace and local AI compute center.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "frontend"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check:naming": "node scripts/check-canonical-naming.mjs",
    "check:runtime-architecture": "node scripts/check-runtime-architecture.mjs"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.34.41",
    "ajv": "^8.17.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
