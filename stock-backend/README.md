# Stock Backend

This directory is reserved for the stock backend.

There is no stock backend implementation in the current codebase yet. The existing stock work is a
frontend view only. Put future stock-specific APIs, data adapters, tests, and deployment files here
so the folder can be published independently from `frontend`, `agent-backend`, and `buddy-backend`.
